<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
  package="com.defenseunicorns.peat_flutter">

    <!-- Bluetooth permissions for Android 12+ (API 31+) -->
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN"
        android:usesPermissionFlags="neverForLocation"
        android:minSdkVersion="31" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT"
        android:minSdkVersion="31" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADVERTISE"
        android:minSdkVersion="31" />

    <!-- Bluetooth permissions for Android 6-11 -->
    <uses-permission android:name="android.permission.BLUETOOTH"
        android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN"
        android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"
        android:maxSdkVersion="30" />

    <!-- INTERNET required for Iroh QUIC relay / direct-connect peers -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Wi-Fi mDNS peer discovery (iroh swarm-discovery via raw multicast).
         Android filters inbound multicast at the Wi-Fi driver to save power;
         the app must hold a WifiManager.MulticastLock to receive it, which
         requires CHANGE_WIFI_MULTICAST_STATE. ACCESS_WIFI_STATE is needed to
         obtain the WifiManager. NEARBY_WIFI_DEVICES (API 33+) gates local
         Wi-Fi peer interaction on Android 13+; neverForLocation since we do
         not derive physical location from it. -->
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_MULTICAST_STATE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.NEARBY_WIFI_DEVICES"
        android:usesPermissionFlags="neverForLocation" />

    <!-- BLE hardware feature: set required="false" if your app degrades gracefully -->
    <uses-feature
        android:name="android.hardware.bluetooth_le"
        android:required="true" />

</manifest>
